const N = 3
range T = 0..N

CINTA = (
		in[a:T] -> PRENSA[a]
		),
PRENSA = PRENSA[0],
PRENSA[a:T] = (
				when(a == 1) entrega -> ENTREGAR
				|when(a > 1) esperar -> PRENSA[a-1]
				|when(a == 0) esperar -> CINTA
			),

ENTREGAR = (proximo -> CINTA).

//||SYNC = (PRENSA(0)||CINTA).
